<h1>New book</h1>
<%= form_with model: @book do |f| %>
<h4>Title</h4>
 <%= f.text_field :title %>
<h4>Opinion</h4>
 <%= f.text_area :body %>
 <%= f.submit 'CreateBook' %>
<% end %>


<% @books.each do |book| %>
 <div>
  <p>ユーザ画像<%= image_tag 'sample-author1.jpg' %></p>
  <a><%= @book.user.name %></a>
  <span><%= link_to book.title,"/books/#{book.id}" %></span>
  <p><%= @book.body %></p>
 </div>

 <% end %>